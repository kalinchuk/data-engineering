= semantic_form_for resource, as: resource_name, url: session_path(resource_name) do |f|
  .container-fluid.no-padding
    .col-xs-4
      .container-fluid
        .form-group
          = f.label :email
          = f.input :email, label: false, input_html: { class: 'form-control' }

      .container-fluid
        .form-group
          = f.label :password
          = f.input :password, as: :password, label: false, input_html: { class: 'form-control' }

      .container-fluid.form-actions
        = f.submit 'Log in', :class => 'btn btn-primary'

    .col-xs-4
      User the following credentials:
      %br
      %br
      
      Email:
      %strong tester@example.com

      %br

      Password:
      %strong 123456