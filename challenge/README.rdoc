== README

To run this application, follow these steps:

```
$ bundle
$ rake db:create
$ rake db:migrate
$ rake db:seed
$ rails s
```

Visit the application at http://localhost:3000 and login with the following information:

```
tester@example.com
123456
```